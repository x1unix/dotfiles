--- Filesystem behavior for file search and file tree.
return {
  -- List if file names to always show and be searchable.
  show_hidden_files = {
    '.github',
    '.gitignore',
    '.gitattributes',
    '.editorconfig',
    '.eslintrc',
    '.npmrc',
  },
}
